extends layouts/main
include mixins/videoBlock

block content 
    .contents
          .recommended-textbox
            span Recommended
          .recommended-videos
            each item in videos
              +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
              })