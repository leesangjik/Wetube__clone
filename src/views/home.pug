extends layouts/main
include mixins/videoBlock

block content
    .contents
        .recommended-title
          span.rc-title Recommended
        .recommended-videos
          each item in videos
            +videoBlock({
              id:item.id,
              title:item.title,
              views:item.views,
              videoFile:item.fileUrl
            })