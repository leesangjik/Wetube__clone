const guideHome = document.getElementById("homeIcon");

const handleHomeIcon = () => {
    //console.dir(guideHome);
}

const init = () => {
    guideHome.addEventListener("mousemove", handleHomeIcon);
};

init();